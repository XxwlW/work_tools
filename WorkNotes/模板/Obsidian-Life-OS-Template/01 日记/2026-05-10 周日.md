---
tags:
  - 日记
心情: Happy
创建时间: 2026-05-10 00:05
---
- [[样例信用卡]] 和 [[小刘]] 一去 去买了实验设备 [[项目A]] #记账 #项目 #贷款 #报销
	- -5000
	- 20261010@-3000#20260830@-2000
## ✅  Tasks 任务

```tasks
not done
(starts on or before today and due on or after today) OR (no due date) OR (no start date)

filter by function\
/* 关键词搜索 关联*/\
  const serch = "";\
  const match = task.description.match(/(\[\[.*?\]\])/);\
  const match2 = (task.file.hasProperty('项目'))  ? task.file.property('项目')[0] : "无关联";\
  const match3 = task.file.filenameWithoutExtension;\
  const final = (match? match[1]:match) ?? match2 ?? match3;\
  const finalb = (final.includes(serch)) || (serch == "");\
  return finalb;

sort by function \
/* 学业优先*/\
	const count = 1;\
	const tagdic = {\
	"约定":1,\
	"学业":22,\
	"想法":333,\
	"问题":4444,\
	};\
	var tag = task.tags[0] ?? "";\
	var tagplus = tagdic[tag] ?? 55555;\
    return count + tagplus
group by function\
  const match = task.description.match(/(\[\[.*?\]\])/);\
  const match2 = (task.file.hasProperty('项目'))  ? task.file.property('项目') : "无关联";\
  const match3 = task.file.filenameWithoutExtension;\
  return (match? match[1]:match) ?? match2 ?? match3

group by function \
  const tag = task.tags[0];\
  const tag2 = task.file.property('tags')[0];\
  return tag ?? tag2 ?? "无tag"

```



## 📕 Diary 日记

```dataviewjs
dv.view("11 scripts/Views/DiaryProfile")
```

### ⚡ Quick Actions

```button
name 记账
type command
action QuickAdd: 记账
color red
```

```button
name 记事
type command
action QuickAdd: 事件
color green
```
